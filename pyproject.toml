[tool.poetry]
name = "saealib"
version = "0.1.0"
description = ""
authors = ["shlka <sazakurosulp36@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
numpy = ">=1.22.1"
scipy = ">=1.0.0"


[tool.poetry.group.dev.dependencies]
opfunu = ">=1.0.0"
setuptools = "^80.9.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
